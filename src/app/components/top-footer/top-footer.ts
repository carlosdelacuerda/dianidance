import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'dd-top-footer',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './top-footer.html',
})
export class TopFooterComponent {}
